# paranoid.py hash files — these live on the backup drive, not in the repo
__paranoid__.json

# Machine-specific config — copy backup.conf.local.example → backup.conf.local
backup.conf.local

# macOS
.DS_Store

# Python
*.pyc
__pycache__/
