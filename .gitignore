# paranoid.py hash files — these live on the backup drive, not in the repo
__paranoid__.json

# Personal config — copy backup.conf.example → backup.conf and fill it in
backup.conf

# macOS
.DS_Store

# Python
*.pyc
__pycache__/
